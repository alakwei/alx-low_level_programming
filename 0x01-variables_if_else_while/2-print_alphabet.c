#include <stdio.h>

/**
 * main : print the alphabets in lower case
 *
 * return: Always 0 (Success)
 *
 */
int main(void)
{
	char ch = '97';

	for (ch = '97'; ch <= '122'; ch++)
		putchar (ch);
			putchar (10);
	return (0);
}
